<div class="background">
    <div class="ripples-container">
        <div class="ripples">
            <div class="ripple"></div>
            <div class="ripple" style="--i: 1;"></div>
            <div class="ripple" style="--i: 2;"></div>
            <div class="ripple" style="--i: 3;"></div>
            <div class="ripple" style="--i: 4;"></div>
        </div>
    </div>
    <div class="fade"></div>
</div>

<style>
.background {
    position: fixed;
    top: 0;
    left: 0;
    display: flex;
    justify-content: center;
    width: 100%;
    height: 200vh;
    background-image: radial-gradient(40% 40% at 92.14% 9.63%, #3A0CA3 0%, #100C43 100%);
    background-repeat: no-repeat;
    z-index: 0;
}

.ripples-container {
    position: relative;
    padding: 0 32px;
    width: 100%;
    max-width: var(--max-width);
}

.ripples {
    position: absolute;
    top: 55.555vw;
    display: grid;
    place-content: center;
    max-width: var(--max-width);
    height: 100vh;
    transform: translateX(-50%) translateY(-50%);
}

.ripple {
    --animation-delay: 0s;
    width: 100vw;
    height: 100vh;
    grid-area: 1/1;
    background-image: radial-gradient(circle closest-side, rgba(105, 92, 255, 0) 41.33%, rgba(105, 92, 255, 0.2) 48.58%, rgba(105, 92, 255, 0) 50%);
    background-repeat: no-repeat;
    animation: ripples 15s ease-out infinite;
    animation-delay: calc(3s * var(--i));
    opacity: 0;
}

.fade {
    position: absolute;
    left: 0;
    width: 100vw;
    height: 200vh;
    grid-area: 1/1;
    background-image: linear-gradient(90deg, rgba(16, 12, 67) 25%, rgba(16, 12, 67, 0) 50%);
    z-index: 1;
}

@media (min-width: 768px) {
    .ripples {
        top: auto;
        transform: translateX(-50%);
        transition: transform 1s ease-in-out;
    }
}

@keyframes ripples {
    0% {
        transform: scale(1.4);
        opacity: 0;
    }
    10% {
        opacity: .75;
    }
    100% {
        transform: scale(7);
        opacity: 0;
    }
}
</style>